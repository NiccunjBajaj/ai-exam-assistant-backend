[tool.poetry]
name = "backend"
version = "0.1.0"
description = ""
authors = ["Niccunj Bajaj"]

[tool.poetry.dependencies]
python = "3.13.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
